// Map - Laver en ny array
/*
const people = [
    {
        name: "Amalie",
        age: 35,
        occupation: "Studerende"
    },
    {
        name: "Julie",
        age: 40,
        occupation: "Studerende"
    },
    {
        name: "Cecilie",
        age: 50,
        occupation: "Studerende"
    },
];

const names = people.map(person => {
    return person.name;
});

console.log(names); // [ 'Amalie', 'Julie', 'Cecilie' ]

// Filter - Filter Amalie og julie fra
const over50 = people.filter(person => {
    return person.age >=50;
}).map(person => {
    return person.name;
});

console.log(over50); // [ 'Cecilie' ]


//Reduce method - reducer en array element.
const totalAge = people.reduce((total, person) => {
    return total + person.age;
}, 0);
console.log(totalAge);
*/

// Exercise 1 - level 1.

